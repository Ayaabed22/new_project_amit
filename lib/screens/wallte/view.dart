import 'package:flutter/material.dart';

class WallteScreen extends StatelessWidget {
  const WallteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}