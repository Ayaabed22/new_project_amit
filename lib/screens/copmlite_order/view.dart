import 'package:flutter/material.dart';

class CompliteOrderScreen extends StatelessWidget {
  const CompliteOrderScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}