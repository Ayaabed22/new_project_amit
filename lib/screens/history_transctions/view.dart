import 'package:flutter/material.dart';

class HistoryTransctionsScreen extends StatelessWidget {
  const HistoryTransctionsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}